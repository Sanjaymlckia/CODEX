# CODEX Hub

Local hub path: `D:\CODEX`

GitHub remote: `https://github.com/Sanjaymlckia/CODEX`

PowerShell launcher: `codexhub`

Notes:
- This repository is the live CODEX hub workspace.
- The hub launcher is implemented by `RUN.ps1`.
- `ZOHO_CRM` lives under `projects\ZOHO_CRM`.
